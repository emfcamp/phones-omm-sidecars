[project]
name = "omm-sidecars"
version = "0.1.0"
description = "Sidecar services for the OpenMobility® Manager (OMM)"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"
